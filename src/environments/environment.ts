/**
 * Production build uses this file by default.
 *
 * Point `apiBaseUrl` at your deployed ASP.NET API root (include `/api` if that is how your app is hosted).
 * Example: https://api.mycompany.com/api
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://YOUR_PRODUCTION_API_HOST/api',
};
